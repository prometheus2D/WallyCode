{
  "id": "execute_tasks",
  "instructions": "Execute the planned tasks and verify the requested work is complete before finishing.",
  "transitions": [
    {
      "selection": "execute_tasks",
      "description": "Continue executing and verifying the planned tasks in the current step.",
      "targetStepName": "execute_tasks"
    }
  ]
}
